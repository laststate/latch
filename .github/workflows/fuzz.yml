name: Fuzz
on:
  schedule:
    - cron: '17 3 * * *'
    - cron: '47 4 * * 0'
  workflow_dispatch:
  pull_request:
    paths: ['src/envelope/**', 'src/security/**', 'src/transport/stream.c', 'include/laststate/envelope.h', 'include/laststate/security.h', 'include/laststate/compression.h', 'include/laststate/stream_transport.h', 'fuzz/**', 'tests/vectors/**', 'tools/seed_fuzz_corpora.py', 'CMakeLists.txt']
permissions:
  contents: read
jobs:
  libfuzzer:
    runs-on: ubuntu-latest
    env:
      CC: clang
      CXX: clang++
      ASAN_OPTIONS: abort_on_error=1:detect_leaks=1
      UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - run: cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=RelWithDebInfo -DLS_BUILD_TESTS=OFF -DLS_BUILD_FUZZERS=ON
      - run: cmake --build build --parallel
      - run: python tools/seed_fuzz_corpora.py --output corpus
      - run: mkdir -p artifacts
      - name: Select fuzzing budget
        run: |
          if [ "${{ github.event.schedule }}" = "47 4 * * 0" ]; then
            echo "FUZZ_SECONDS=3600" >> "$GITHUB_ENV"
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            echo "FUZZ_SECONDS=900" >> "$GITHUB_ENV"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "FUZZ_SECONDS=600" >> "$GITHUB_ENV"
          else
            echo "FUZZ_SECONDS=120" >> "$GITHUB_ENV"
          fi
      - run: build/fuzz-lep corpus/lep -artifact_prefix=artifacts/ -max_total_time=${FUZZ_SECONDS} -timeout=5
      - run: build/fuzz-compression corpus/compression -artifact_prefix=artifacts/ -max_total_time=${FUZZ_SECONDS} -timeout=5
      - run: build/fuzz-aead corpus/aead -artifact_prefix=artifacts/ -max_total_time=${FUZZ_SECONDS} -timeout=5
      - run: build/fuzz-stream corpus/stream -artifact_prefix=artifacts/ -max_total_time=${FUZZ_SECONDS} -timeout=5
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        if: always()
        with:
          name: fuzz-corpus-and-crashes
          path: |
            corpus
            artifacts
